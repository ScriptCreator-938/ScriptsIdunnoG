local TeleportService = game:GetService("TeleportService")
local currentPlaceId = game.PlaceId

local WORLD_1_ID = 11455876520
local WORLD_2_ID = 4072828011

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ChooseScriptGui"
screenGui.ResetOnSpawn = false

local player = game:GetService("Players").LocalPlayer
screenGui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Name = "MainFrame"
frame.Size = UDim2.fromOffset(280, 200)
frame.Position = UDim2.new(0.5, -140, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 46)
frame.BorderSizePixel = 0
frame.ClipsDescendants = true
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 10)
frameCorner.Parent = frame

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.fromOffset(210, 35)
titleLabel.Position = UDim2.fromOffset(15, 5)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Choose a Script"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 18
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = frame

local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Name = "MinimizeBtn"
minimizeBtn.Size = UDim2.fromOffset(28, 28)
minimizeBtn.Position = UDim2.fromOffset(242, 8)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
minimizeBtn.Text = "-"
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.TextSize = 20
minimizeBtn.Font = Enum.Font.SourceSansBold
minimizeBtn.Parent = frame

local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(0, 6)
minCorner.Parent = minimizeBtn

local openBtn = Instance.new("TextButton")
openBtn.Name = "OpenGuiBtn"
openBtn.Size = UDim2.fromOffset(45, 45)
openBtn.Position = UDim2.new(0, 15, 0.5, -22)
openBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 46)
openBtn.Text = "📜"
openBtn.TextSize = 20
openBtn.Visible = false
openBtn.Parent = screenGui

local openCorner = Instance.new("UICorner")
openCorner.CornerRadius = UDim.new(1, 0)
openCorner.Parent = openBtn

local btnWorld1 = Instance.new("TextButton")
btnWorld1.Name = "BtnWorld1"
btnWorld1.Size = UDim2.fromOffset(250, 35)
btnWorld1.Position = UDim2.fromOffset(15, 45)
btnWorld1.BackgroundColor3 = Color3.fromRGB(137, 180, 250)
btnWorld1.Text = "TSS Script World 1 - NoneUserHub"
btnWorld1.TextColor3 = Color3.fromRGB(17, 17, 27)
btnWorld1.Font = Enum.Font.SourceSansBold
btnWorld1.TextSize = 13
btnWorld1.Parent = frame

local btn1Corner = Instance.new("UICorner")
btn1Corner.CornerRadius = UDim.new(0, 8)
btn1Corner.Parent = btnWorld1

local labelHere1 = Instance.new("TextLabel")
labelHere1.Name = "LabelHere1"
labelHere1.Size = UDim2.fromOffset(250, 15)
labelHere1.Position = UDim2.fromOffset(15, 82)
labelHere1.BackgroundTransparency = 1
labelHere1.Text = "You're here!"
labelHere1.TextColor3 = Color3.fromRGB(46, 204, 113)
labelHere1.Font = Enum.Font.SourceSansBold
labelHere1.TextSize = 13
labelHere1.Visible = (currentPlaceId == WORLD_1_ID)
labelHere1.Parent = frame

local btnWorld2 = Instance.new("TextButton")
btnWorld2.Name = "BtnWorld2"
btnWorld2.Size = UDim2.fromOffset(250, 35)
btnWorld2.Position = UDim2.fromOffset(15, 110)
btnWorld2.BackgroundColor3 = Color3.fromRGB(166, 227, 161)
btnWorld2.Text = "TSS Script World 2 - NoneUserHub"
btnWorld2.TextColor3 = Color3.fromRGB(17, 17, 27)
btnWorld2.Font = Enum.Font.SourceSansBold
btnWorld2.TextSize = 13
btnWorld2.Parent = frame

local btn2Corner = Instance.new("UICorner")
btn2Corner.CornerRadius = UDim.new(0, 8)
btn2Corner.Parent = btnWorld2

local labelHere2 = Instance.new("TextLabel")
labelHere2.Name = "LabelHere2"
labelHere2.Size = UDim2.fromOffset(250, 15)
labelHere2.Position = UDim2.fromOffset(15, 147)
labelHere2.BackgroundTransparency = 1
labelHere2.Text = "You're here!"
labelHere2.TextColor3 = Color3.fromRGB(46, 204, 113)
labelHere2.Font = Enum.Font.SourceSansBold
labelHere2.TextSize = 13
labelHere2.Visible = (currentPlaceId ~= WORLD_1_ID)
labelHere2.Parent = frame

local confirmFrame = Instance.new("Frame")
confirmFrame.Name = "ConfirmFrame"
confirmFrame.Size = UDim2.fromOffset(300, 160)
confirmFrame.Position = UDim2.new(0.5, -150, 0.5, -80)
confirmFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 37)
confirmFrame.BorderSizePixel = 0
confirmFrame.Visible = false
confirmFrame.Active = true
confirmFrame.Draggable = true
confirmFrame.Parent = screenGui

local confirmCorner = Instance.new("UICorner")
confirmCorner.CornerRadius = UDim.new(0, 10)
confirmCorner.Parent = confirmFrame

local confirmTitle = Instance.new("TextLabel")
confirmTitle.Name = "ConfirmTitle"
confirmTitle.Size = UDim2.fromOffset(270, 60)
confirmTitle.Position = UDim2.fromOffset(15, 15)
confirmTitle.BackgroundTransparency = 1
confirmTitle.Text = "Are you sure you gonna execute\n[Tên nút]?"
confirmTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
confirmTitle.TextSize = 15
confirmTitle.Font = Enum.Font.SourceSansBold
confirmTitle.TextWrapped = true
confirmTitle.Parent = confirmFrame

local btnYes = Instance.new("TextButton")
btnYes.Name = "BtnYes"
btnYes.Size = UDim2.fromOffset(125, 35)
btnYes.Position = UDim2.fromOffset(15, 95)
btnYes.BackgroundColor3 = Color3.fromRGB(243, 139, 168)
btnYes.Text = "Yes"
btnYes.TextColor3 = Color3.fromRGB(17, 17, 27)
btnYes.Font = Enum.Font.SourceSansBold
btnYes.TextSize = 15
btnYes.Parent = confirmFrame

local yesCorner = Instance.new("UICorner")
yesCorner.CornerRadius = UDim.new(0, 8)
yesCorner.Parent = btnYes

local btnNo = Instance.new("TextButton")
btnNo.Name = "BtnNo"
btnNo.Size = UDim2.fromOffset(125, 35)
btnNo.Position = UDim2.fromOffset(160, 95)
btnNo.BackgroundColor3 = Color3.fromRGB(108, 112, 134)
btnNo.Text = "No"
btnNo.TextColor3 = Color3.fromRGB(255, 255, 255)
btnNo.Font = Enum.Font.SourceSansBold
btnNo.TextSize = 15
btnNo.Parent = confirmFrame

local noCorner = Instance.new("UICorner")
noCorner.CornerRadius = UDim.new(0, 8)
noCorner.Parent = btnNo

local targetScriptUrl = ""

minimizeBtn.MouseButton1Click:Connect(function()
    frame.Visible = false
    openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
    frame.Visible = true
    openBtn.Visible = false
end)

btnNo.MouseButton1Click:Connect(function()
    confirmFrame.Visible = false
    frame.Visible = true
end)

btnYes.MouseButton1Click:Connect(function()
    if targetScriptUrl ~= "" then
        screenGui:Destroy()
        loadstring(game:HttpGet(targetScriptUrl))()
    end
end)

local SCRIPT_W1_URL = "https://raw.githubusercontent.com/ScriptCreator-938/ScriptsIdunnoG/main/TSS%20Script%20World%201"
local SCRIPT_W2_URL = "https://raw.githubusercontent.com/ScriptCreator-938/ScriptsIdunnoG/main/TSS%20Script%20World%202"

btnWorld1.MouseButton1Click:Connect(function()
    if currentPlaceId == WORLD_1_ID then
        screenGui:Destroy()
        loadstring(game:HttpGet(SCRIPT_W1_URL))()
    else
        targetScriptUrl = SCRIPT_W1_URL
        confirmTitle.Text = "Are you sure you gonna execute\n" .. btnWorld1.Text .. "?"
        frame.Visible = false
        confirmFrame.Visible = true
    end
end)

btnWorld2.MouseButton1Click:Connect(function()
    if currentPlaceId == WORLD_2_ID or currentPlaceId ~= WORLD_1_ID then
        screenGui:Destroy()
        loadstring(game:HttpGet(SCRIPT_W2_URL))()
    else
        targetScriptUrl = SCRIPT_W2_URL
        confirmTitle.Text = "Are you sure you gonna execute\n" .. btnWorld2.Text .. "?"
        frame.Visible = false
        confirmFrame.Visible = true
    end
end)